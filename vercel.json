{
  "framework": "nextjs",
  "buildCommand": "SKIP_IMAGE_CHECK=1 rm -rf .next/cache && npm run build",
  "outputDirectory": ".next",
  "installCommand": "npm install",
  "regions": ["fra1"],
  "crons": [
    { "path": "/api/cron/daily",        "schedule": "0 8 * * *"  },
    { "path": "/api/cron/orchestrator", "schedule": "0 6 * * *"  }
  ]
}
